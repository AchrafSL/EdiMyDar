<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EdiMyDar" parent="Theme.Material3.Light.NoActionBar">


        <!-- Define a custom button style -->
        <item name="materialButtonStyle">@style/CustomButtonStyle</item>
        <item name="colorSecondary">@null</item>

    </style>

    <style name="Theme.EdiMyDar" parent="Base.Theme.EdiMyDar" />

    <style name="Theme.AppWithActionBar" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme -->
        <item name="colorPrimary">@color/limouniKhfif</item>
    </style>

    <style name="Theme.AppWithoutActionBar" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme -->
    </style>

    <!-- Custom Button Style -->
    <style name="CustomButtonStyle" parent="Widget.Material3.Button">
        <item name="backgroundTint">@null</item> <!-- Disable default background tint -->
        <item name="backgroundColor">@null</item>
    </style>

    <style name="CustomActiveIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/limouniKhfif</item>
    </style>
</resources>